apiVersion: apps/v1
kind: Deployment
metadata:
  name: prod-reports-api
  namespace: reports-production
  labels:
    app: prod-reports
spec:
  replicas: 1
  selector:
    matchLabels:
      app: prod-reports
  template:
    metadata:
      labels:
        app: prod-reports
    spec:
      imagePullSecrets:
        - name: regcred
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
      containers:
        - name: reports-api
          image: anrs125/reports-tesing:v1.2.9
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: reports-config
            - secretRef:
                name: reports-secrets
          args:
            - --log-level=info
            - --scheme=http
            - --port=8000
            - --host=0.0.0.0
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: prod-reports
  namespace: reports-production
  labels:
    app: prod-reports
spec:
  selector:
    app: prod-reports
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 31001
  type: NodePort
